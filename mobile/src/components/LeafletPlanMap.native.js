/**
 * Native build'de bu dosya YÜKLENMEZ — sadece web için.
 * Metro bundler `.web.js` uzantısını web platform'da seçer.
 * Bu dosya stub olarak kalır ki import çakışması olmasın.
 */
export default function LeafletPlanMap() {
  return null;
}
